<!-- The contents of this file will be loaded for each web application -->
<Context path="/EvaluateMe" docBase="EvaluateMe" reloadable="true">
	<!-- EvaluateMe -->
<!-- 	<Resource 
		name="${context.db.evaluateme.name}" 
		auth="${context.db.evaluateme.auth}" 
        type="${context.db.evaluateme.type}" 
		username="${context.db.evaluateme.username}" 
		password="${context.db.evaluateme.password}" 
        driverClassName="${context.db.evaluateme.driverClassName}" 
		url="${context.db.evaluateme.url}" 
        maxActive="${context.db.evaluateme.maxActive}" 
		maxIdle="${context.db.evaluateme.maxIdle}" 
		removeAbandoned="${context.db.evaluateme.removeAbandoned}" 
		removeAbandonedTimeout="${context.db.evaluateme.removeAbandonedTimeout}" 
		logAbandoned="${context.db.evaluateme.logAbandoned}" 
	/> -->
</Context>
